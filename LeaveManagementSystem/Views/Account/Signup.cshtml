@model LeaveManagementSystem.Models.SignupModel
@{
    Layout = null;
    ViewBag.Title = "Sign Up";
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up - Leave Management System</title>
    <link rel="stylesheet" href="~/css/site.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
</head>
<body class="signup-page">
    <div class="signup-container">
        @if (TempData["Message"] != null)
        {
            <div class="alert alert-success">
                @TempData["Message"]
            </div>
        }

        @if (TempData["Error"] != null)
        {
            <div class="alert alert-error">
                @TempData["Error"]
            </div>
        }
        <div class="signup-header">
            <h1>Create Account</h1>
            <p>Sign up to get started</p>
        </div>

        <form method="post" action="/Account/Signup" enctype="multipart/form-data" autocomplete="off">
            @Html.HiddenFor(m => m.UserId)
            @Html.AntiForgeryToken()
            @Html.ValidationSummary(true, "", new { @class = "validation-summary" })

            <div class="form-group">
                <label>Full Name</label>
                @Html.TextBoxFor(m => m.FullName, new { @class = "form-control", placeholder = "Enter your full name" })
                @Html.ValidationMessageFor(m => m.FullName, "", new { @class = "field-validation-error" })
            </div>

            <div class="form-group">
                <label>Mobile Number</label>
                @Html.TextBoxFor(m => m.MobileNo, new { @class = "form-control", type = "text", maxlength = "10", placeholder = "Enter Mobile Number" })
                @Html.ValidationMessageFor(m => m.MobileNo, "", new { @class = "field-validation-error" })
            </div>

            <div class="form-group">
                <label>Email</label>
                @Html.TextBoxFor(m => m.Email, new { @class = "form-control", type = "email", placeholder = "Enter your email", autocomplete = "off" })
                @Html.ValidationMessageFor(m => m.Email, "", new { @class = "field-validation-error" })
            </div>

            <div class="form-group password-wrapper">
                <label>Password</label>
                <div style="position:relative;">
                    @Html.PasswordFor(m => m.Password, new { @class = "form-control", placeholder = "Enter password", autocomplete = "new-password", id = "Password" })
                    <span class="toggle-password" data-target="Password"><i class="fa-regular fa-eye"></i></span>
                </div>
                @Html.ValidationMessageFor(m => m.Password, "", new { @class = "field-validation-error" })
            </div>

            <div class="form-group password-wrapper">
                <label>Confirm Password</label>
                <div style="position:relative;">
                    @Html.PasswordFor(m => m.ConfirmPassword, new { @class = "form-control", placeholder = "Confirm password", id = "ConfirmPassword" })
                    <span class="toggle-password" data-target="ConfirmPassword"><i class="fa-regular fa-eye"></i></span>
                </div>
                @Html.ValidationMessageFor(m => m.ConfirmPassword, "", new { @class = "field-validation-error" })
            </div>

            <div class="form-group">
                <label>Date of Birth</label>
                @Html.TextBoxFor(m => m.DateOfBirth, "{0:yyyy-MM-dd}", new { @class = "form-control", type = "date", id = "DateOfBirth" })
                @Html.ValidationMessageFor(m => m.DateOfBirth, "", new { @class = "field-validation-error" })
            </div>

            <div class="form-group">
                <label>Age</label>
                <input type="text" id="Age" class="form-control" readonly placeholder="Age will be calculated automatically" />
            </div>

            <div class="form-group">
                <label>Salary</label>
                @Html.TextBoxFor(m => m.Salary, new { @class = "form-control", step = "0.01", type = "number" })
                @Html.ValidationMessageFor(m => m.Salary)
            </div>

            <div class="form-group">
                <label>Joining Date</label>
                @Html.TextBoxFor(m => m.JoiningDate, "{0:yyyy-MM-dd}", new { @class = "form-control", type = "date" })
                @Html.ValidationMessageFor(m => m.JoiningDate)
            </div>

            <div class="form-group">
                <label>Gender</label>
                <div>
                    <label style="margin-left:20px;">
                        @Html.RadioButtonFor(m => m.Gender, "Male") Male
                    </label>
                    <label style="margin-left:20px;">
                        @Html.RadioButtonFor(m => m.Gender, "Female") Female
                    </label>
                    <label style="margin-left:20px;">
                        @Html.RadioButtonFor(m => m.Gender, "Other") Other
                    </label>
                </div>
                @Html.ValidationMessageFor(m => m.Gender)
            </div>

            <div class="form-group">
                <label>Department</label>
                @Html.DropDownListFor(m => m.DepartmentId, Model.DepartmentList, "Select Department", new { @class = "form-control" })
                @Html.ValidationMessageFor(m => m.DepartmentId, "", new { @class = "field-validation-error" })
            </div>

            <div class="form-group">
                <label>Role</label>
                @Html.DropDownListFor(m => m.Role, Model.RoleList, "Select Role", new { @class = "form-control" })
            </div>

            <div class="form-group">
                <label>Skills</label>
                <div>
                    <label style="margin-left:20px;">
                        <input type="checkbox" name="Skills" value="C#" @(Model.Skills.Contains("C#") ? "checked" : "") /> C#
                    </label>
                    <label style="margin-left:20px;">
                        <input type="checkbox" name="Skills" value="SQL" @(Model.Skills.Contains("SQL") ? "checked" : "") /> SQL
                    </label>
                    <label style="margin-left:20px;">
                        <input type="checkbox" name="Skills" value="MVC" @(Model.Skills.Contains("MVC") ? "checked" : "") /> MVC
                    </label>
                    <label style="margin-left:20px;">
                        <input type="checkbox" name="Skills" value="JavaScript" @(Model.Skills.Contains("JavaScript") ? "checked" : "") /> JavaScript
                    </label>
                </div>
                @Html.ValidationMessageFor(m => m.Skills,"", new { @class = "field-validation-error" })
            </div>

            <div class="form-group">
                <label>Address</label>
                @Html.TextAreaFor(m => m.Address, 5, 30, new { @class = "form-control", placeholder = "Enter Address" })
                @Html.ValidationMessageFor(m => m.Address)
            </div>

            <div class="form-group">
                <label>Profile Image</label>
                @Html.ValidationMessageFor(m => m.ProfileImage, "", new { @class = "field-validation-error" })
                @Html.TextBoxFor(m => m.ProfileImage,new{ type = "file", @class = "form-control", accept = ".jpg,.jpeg,.png"})
            </div>

            <button type="submit" class="btn-signup">Sign Up</button>
            <input type="reset" value="Reset" class="btn-signup" style="background:red;margin-top:10px;" />
        </form>

        <div class="login-link">
            Already have an account? <a href="/Account/Login">Login here</a>
        </div>
    </div>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="/js/site.js"></script>
</body>
</html>
